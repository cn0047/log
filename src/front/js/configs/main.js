/**
 * General config.
 */
module.exports = {
  // Default auto-scrolling value.
  autoScroll: true,

  // Determines whether GoogleAnalytics is enabled.
  googleAnalyticsEnabled: true,

  // After this threshold counter appears on main page.
  thresholdToDisplayCounter: 10,
};
